# BugCrusher Hunt Report

**Session:** `4cb9f1bb`  
**Target:** `atlassian`  
**Date:** `2026-04-18 00:10:00`  
**Duration:** `0.02h`

---

## Worm Status
🚫 **WORM: CLEAN** — 0 suspicious artifacts detected

---

## Findings Summary

| # | Type | Severity | Endpoint | CVSS | Status |
|---|------|----------|----------|------|--------|
| 1 | Apollo Tracing Exposed | MEDIUM | `https://marketplace.atlassian.com/gateway/api/graphql` | 4.3 | P5 — Info Only |
| 2 | GraphQL Introspection | INFO | `https://marketplace.atlassian.com/gateway/api/graphql` | 0.0 | Out of Scope |

---

## Finding #1 — Apollo Tracing Enabled

**🏷️ MEDIUM** | CVSS 4.3 | `https://marketplace.atlassian.com/gateway/api/graphql`

### Description
Apollo Tracing is enabled on the Atlassian Marketplace GraphQL endpoint, exposing resolver-level performance metrics including:
- Field-level latency measurements
- Internal resolver function names
- Execution plan structure
- Internal architecture topology

### PoC
```bash
curl -s -X POST "https://marketplace.atlassian.com/gateway/api/graphql" \
  -H "Content-Type: application/json" \
  -d '{"operationName":"Test","query":"query Test{ __schema { queryType { name } } }"}'
```

Response includes `extensions: { tracing: { version: ... } }` header with resolver performance data.

### Impact Assessment
**Medium** — Exposes internal GraphQL architecture including resolver chain, field names, and execution timing. Can aid attackers in understanding the API structure for targeted attacks.

**Note:** Per program scope, this is informational only. GraphQL endpoint info disclosure is explicitly out of scope for Atlassian Bug Bounty.

---

## Finding #2 — GraphQL Introspection Enabled (P5)

**ℹ️ INFO** | CVSS 0.0 | `https://marketplace.atlassian.com/gateway/api/graphql`

### Description
Full GraphQL introspection accessible without authentication. Schema exposes **25,747 types** including:
- `User`, `Admin`, `Query`, `Mutation`, `Subscription`
- Internal Atlassian product types (Jira, Confluence, Compass, etc.)
- Marketplace-specific types (billing, licensing, product listings)

### PoC
```bash
curl -s -X POST "https://marketplace.atlassian.com/gateway/api/graphql" \
  -H "Content-Type: application/json" \
  -d '{"operationName":"IntrospectionQuery","query":"{ __schema { types { name kind fields { name } } } }"}'
```

### Impact Assessment
**Informational** — Schema enumeration only. No data access. Per program rules, GraphQL introspection is explicitly out of scope.

---

## Other Endpoints Tested

| Endpoint | HTTP Code | Notes |
|----------|-----------|-------|
| `https://id.atlassian.com/graphql` | 302 → 403 | CloudFront blocking POST |
| `https://admin.atlassian.com` | 200 | Login portal |
| `https://docs.atlassian.com` | 200 | Documentation |
| `https://support.atlassian.com` | 200 | Support portal |
| `https://test.atlassian.net/wiki` | 401 | Requires auth |
| `https://compass.atlassian.com/gateway/api/graphql` | 000 | Connection refused |

---

## CVEs Weaponized Against Target Stack

| CVE | CVSS | Target | Status |
|-----|------|--------|--------|
| CVE-2023-22515 (Confluence RCE) | 9.1 | *.atlassian.net | Auth required |
| CVE-2024-3400 (PAN-OS RCE) | 10.0 | Network devices | Not in scope |
| CVE-2024-21762 (Fortinet VPN) | 9.8 | SSL VPN | Not in scope |

---

## Session Hijacker Results

```
Target: https://id.atlassian.com
Cookies Found: 0
Auth Bypass: No (200 OK on public endpoints only)
```

---

## Conclusion

**All findings are P5 / Info.** No exploitable vulnerabilities found without authenticated test instance.

### Recommended Actions
1. **Wait for test instance** — Atlassian requires `bugbounty-test-<username>.atlassian.net` for Forge app testing
2. **Focus areas for authenticated testing:**
   - Forge SSRF via `forge.fetch()` → AWS metadata
   - Cross-workspace IDOR in Jira/Confluence
   - Rovo AI Agent prompt injection
3. **Re-test when credentials available**

---

*Generated by BugCrusher v3.5 — 2026-04-18 00:10:00*
